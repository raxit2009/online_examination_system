<!DOCTYPE html>
<html>
<head>
	<title>Quiz</title>
</head>
<body>
<h1>Available Quiz</h1>
<table>
	<tr>
		<th>Quiz Type</th>
		<th>Link</th>
	</tr>
	<% @quiz_type.each do |quiz| %>
		<tr>
			<td><%=quiz.title %></td>
			<td><%= link_to "Start Test", start_quiz_path(quiz.id)%></td>
		</tr>
	<% end %>
</table>
</body>
</html>